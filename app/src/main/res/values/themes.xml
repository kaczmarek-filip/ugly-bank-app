<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Bank" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/main</item> <!-- Ustaw kolor paska stanu -->
        <item name="android:navigationBarColor">@color/main</item> <!-- (opcjonalne) Ustaw kolor paska nawigacyjnego (dolnego) -->
    </style>

    <style name="Theme.Bank" parent="Base.Theme.Bank" />
</resources>